# Configuration Files

## Training Configurations
- `act_config.yaml` - ACT model training configuration
- `smolvla_v5_config.yaml` - SmolVLA v5 training configuration
- `smolvla_v6_config.yaml` - SmolVLA v6 training configuration (optimal)

## Robot Configurations
- `so101_follower_config.yaml` - SO-101 follower arm configuration
- `camera_config.yaml` - Camera setup and device mappings

*Configuration files will be extracted from training runs*
